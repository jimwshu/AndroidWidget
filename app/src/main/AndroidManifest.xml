<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidwidget.androidwidget" >

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--
 To retrieve OAuth 2.0 tokens or invalidate tokens to disconnect a user. This disconnect
     option is required to comply with the Google+ Sign-In developer policies
    -->
    <uses-permission android:name="android.permission.USE_CREDENTIALS" /> <!-- To retrieve the account name (email) as part of sign-in: -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" /> <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="ANDROID.PERMISSION.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UserInfoListActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".CustomCameraActivity"
            android:label="@string/title_activity_custom_camera" >
        </activity>
        <activity
            android:name=".PrimaryColorActivity"
            android:label="@string/title_activity_primary_color" >
        </activity>
        <activity
            android:name=".ImageXYChange"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".ImagePickerActivity"
            android:label="@string/title_activity_image_picker" >
        </activity>

        <activity
                android:name=".TextViewActionAnimationActivity"
                android:label="@string/title_activity_image_picker" >
        </activity>
        <activity
            android:name=".CustomView.GestureLockViewGroup"
            android:label="@string/title_activity_gesture_lock_view_group" >
        </activity>
    </application>

</manifest>
